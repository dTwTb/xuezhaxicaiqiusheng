<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finaltest.myapplication">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".web_xiugaiActivity" />
        <activity android:name=".web_viewActivity" />
        <activity android:name=".web_tianjiaActivity" />
        <activity android:name=".webActivity" />

        <receiver
            android:name=".courseReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".course_xiugaiActivity" />
        <activity android:name=".course_viewActivity" />
        <activity android:name=".course_tianjiaActivity" />
        <activity android:name=".courseActivity" />

        <receiver android:name=".Tongzhi_ddlActivity" />

        <activity android:name=".ddlxiugaiActivity" />
        <activity android:name=".ddl_viewActivity" />
        <activity android:name=".ddlTianjiaActivity" />
        <activity android:name=".ddlActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>